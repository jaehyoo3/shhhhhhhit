<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="withdrawLog">

    <!-- ResultMap 정의 -->
    <resultMap id="withdrawLogResultMap" type="WithdrawLog">
        <id property="logId" column="log_id"/>
        <result property="userId" column="user_id"/>
        <result property="originalEmail" column="original_email"/>
        <result property="reason" column="reason"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- withdrawLog.insert: 탈퇴 로그 등록 -->
    <insert id="insert" parameterType="WithdrawLog" useGeneratedKeys="true" keyProperty="logId">
        INSERT INTO withdraw_logs (
            user_id, original_email, reason
        ) VALUES (
            #{userId}, #{originalEmail}, #{reason}
        )
    </insert>

</mapper>

